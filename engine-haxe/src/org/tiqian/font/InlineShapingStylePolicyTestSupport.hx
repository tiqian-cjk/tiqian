package org.tiqian.font;

class InlineShapingStylePolicyTestSupport {
    public static function vals(n:Int):Array<String> {
        var a:Array<String> = [];
        var i = 0;
        while (i < n) {
            a.push("value");
            i++;
        }
        return a;
    }
}
